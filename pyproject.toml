[project]
name = "llms"
version = "0.1.0"
description = "Esempi dalla serie Neural Networks: Zero to Hero di Karpathy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "torch>=2.13.0",
]
